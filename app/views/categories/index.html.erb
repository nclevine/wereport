<h2>Categories</h2>
<div class='categories'>
  <% @categories.each do |category| %>
    <div class='category'>
      <h3><%= link_to category.name, category %></h3>
      <p><%= category.stories.length %> stories</p>
    </div>
  <% end %>
</div>