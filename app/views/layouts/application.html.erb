<!DOCTYPE html>
<html>
<head>
  <title>WeReport</title>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <link href='https://fonts.googleapis.com/css?family=Goudy+Bookletter+1911' rel='stylesheet' type='text/css'>
  <link href='https://fonts.googleapis.com/css?family=Ropa+Sans' rel='stylesheet' type='text/css'>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>
<body>
  <header>
    <div class='logo-new'>
      <h1><%= link_to "WeReport", root_path %></h1>
      <h2><%= link_to "✍", new_story_path %></h2>
      <h3>Crowdsourced. Local. Immediate.</h3>
    </div>
    <div class='sign-in-out'>
      <% if user_signed_in? %>
        <h3>Signed in as: <span class='username'><%= current_user.username %></span></h3>
        <h3><%= link_to 'Signout', destroy_user_session_path, :method => :delete %></h3>
      <% else %> 
        <h3><%= link_to 'Signin', new_user_session_path %></h3>
      <% end %>
    </div>
    <div class='clear'></div>
    <div class='cat-nav-container'>
      <div class='categories-nav'>
        <% Category.all.each do |category| %>
          <h2><%= link_to category.name, category %></h2> 
        <% end %>
      </div>
    </div>
    <div class='clear'></div>
  </header>

  <%= yield %>

  <footer>
    <p>© N. Levine, P. Singh, E. Shin</p>
  </footer>
</body>
</html>
